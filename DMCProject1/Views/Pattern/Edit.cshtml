@model DMCProject1.Helpers.PatternCollection

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>Pattern</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.pattern.PatternId)

    <div class="form-group">
        @Html.LabelFor(model => model.pattern.Name, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.EditorFor(model => model.pattern.Name, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.pattern.Name, "", new { @class = "text-danger" })
        </div>
    </div>


    @*<div class="form-group">
           @Html.LabelFor(model => model.colors.DmcId, htmlAttributes: new { @class = "control-label col-md-2" })
           <div class="col-md-10">
               @Html.EditorFor(model => model.colors.DmcId, new { htmlAttributes = new { @class = "form-control" } })
               @Html.ValidationMessageFor(model => model.colors.DmcId, "", new { @class = "text-danger" })
           </div>
        <div> *@

    <table id="colors">
        <tbody>
            @foreach (var color in Model.colors)
            {
                @Html.Partial("ColorRow", color)
            }
        </tbody>
    </table>

    <button type="button" id="addColor">Add Color</button>

    <script>
        $("#addColor").on("click", function () {
            $.ajax({
                type: "POST",
                url: '@Url.Action("AddColor", "Pattern")',
                success: function (partialView) {
                    $('#colors> tbody:last-child').append(partialView);
                }
            });
        });
    </script>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
</div>
        }

        <div>
            @Html.ActionLink("Back to List", "Index")
        </div>

        @section Scripts {
            @Scripts.Render("~/bundles/jqueryval")
        }
